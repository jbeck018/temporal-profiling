apiVersion: 1

providers:
  - name: 'Temporal Profiler'
    orgId: 1
    folder: 'Temporal'
    folderUid: 'temporal'
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards
