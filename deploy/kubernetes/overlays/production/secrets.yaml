# IMPORTANT: Replace with actual secrets management (External Secrets, Sealed Secrets, etc.)
# This is a template - DO NOT commit real secrets to version control

apiVersion: v1
kind: Secret
metadata:
  name: temporal-profiler-secrets
  namespace: temporal-profiler-prod
type: Opaque
stringData:
  # Replace with your actual Slack webhook URL
  slack-webhook-url: "https://hooks.slack.com/services/YOUR/WEBHOOK/URL"

---
# Example using External Secrets Operator with AWS Secrets Manager
# Uncomment and configure for production use
#
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: temporal-profiler-secrets
#   namespace: temporal-profiler-prod
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     name: aws-secretsmanager
#     kind: ClusterSecretStore
#   target:
#     name: temporal-profiler-secrets
#     creationPolicy: Owner
#   data:
#     - secretKey: slack-webhook-url
#       remoteRef:
#         key: temporal-profiler/slack-webhook-url
